package object config:
  /** Configuration — загрузка настроек из HOCON/application.conf.
    *
    * Параметры:
    *   - Server (host, port)
    *   - Database (url, user, password, poolSize)
    *   - Kafka (bootstrapServers, groupId)
    *   - Logging (level, format)
    */
  ()
