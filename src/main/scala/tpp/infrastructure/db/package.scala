package object db:
  /** Database adapters — PostgreSQL через Doobie.
    *
    * Компоненты:
    *   - EventStorePostgres — Event Store
    *   - TransactionRepository — ReadModel для транзакций
    *   - AccountRepository — ReadModel для счетов
    *   - Projections — обработчики событий
    */
  ()
